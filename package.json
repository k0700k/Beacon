{
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start:backend": "powershell -Command \"cd backend; .\\venv\\Scripts\\uvicorn app.main:app --reload\""
  },
  "dependencies": {
    "firebase": "^12.15.0"
  }
}
